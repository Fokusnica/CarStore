<section>
  <div class="container">
    <div class="row">
      <%=render 'categories' %>
      <div class="col-sm-9 padding-right">
        <div class="features_items"><!--features_items-->
          <h2 class="title text-center"><%=t('.models_in_category')%> : </h2>

          <div id="product">
            <%= render 'products' %>
          </div>

        </div>
        <!--features_items-->
        <div id="paginator">
          <%= paginate @products, :window => 1, :remote => true %>
        </div>
      </div>
    </div>
  </div>
</section>
